/* vim: set filetype=sh: */

# Load auto completions
if [ -f "$(brew --prefix)/etc/bash_completion" ]; then
    source "$(brew --prefix)/etc/bash_completion"
else
    echo "Bash auto completions are not installed."
fi
